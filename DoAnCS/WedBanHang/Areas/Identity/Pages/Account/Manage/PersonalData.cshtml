@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Dữ liệu cá nhân";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

    <div class="col-md-8">
        <h4 class="mb-3 text-danger fw-bold"><i class="fas fa-user-lock me-2"></i>@ViewData["Title"]</h4>
        <div class="alert alert-warning small">
            <p><strong>⚠️ Tài khoản của bạn chứa dữ liệu cá nhân mà bạn đã cung cấp.</strong></p>
            <p>Trang này cho phép bạn <strong>tải xuống</strong> hoặc <strong>xóa</strong> toàn bộ dữ liệu.</p>
            <p class="mb-0 text-danger"><strong>Lưu ý: Việc xóa sẽ xóa vĩnh viễn tài khoản và không thể khôi phục!</strong></p>
        </div>

        <form id="download-data" asp-page="DownloadPersonalData" method="post" class="mb-3">
            <button class="btn btn-outline-primary w-100" type="submit">
                <i class="fas fa-download me-2"></i> Tải dữ liệu về
            </button>
        </form>

        <a id="delete" asp-page="DeletePersonalData" class="btn btn-outline-danger w-100">
            <i class="fas fa-user-slash me-2"></i> Xóa tài khoản
        </a>
    </div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
